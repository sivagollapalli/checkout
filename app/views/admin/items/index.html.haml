.panel.panel-default 
  .panel-heading
    .panel-title Items 
  .panel-body

    .row
      .col-md-6
        %table.table
          %thead
            %tr
              %th Name
              %th
                .pull-right
                  Price
              %th
              %th

          %tbody
            - @items.each do |item|
              %tr
                %td= item.name
                %td
                  .pull-right= number_to_currency(item.price, unit: '£')
                %td
                %td= link_to 'Edit', edit_admin_item_path(item), class: 'btn btn-primary btn-xs'

        %br
    .row
      .col-md-6
        .pull-right
          = link_to 'New Item', new_admin_item_path, class: 'btn btn-primary'
