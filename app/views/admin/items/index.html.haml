%h1 Listing items

%table
  %thead
    %tr
      %th Name
      %th Price
      %th
      %th
      %th

  %tbody
    - @items.each do |item|
      %tr
        %td= item.name
        %td= item.price
        %td= link_to 'Show', admin_item_path(item)
        %td= link_to 'Edit', edit_admin_item_path(item)
        %td= link_to 'Destroy', admin_item_path(item), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Item', new_admin_item_path
