- if @order
  $("#order_summary").html("#{escape_javascript(render 'order_summary')}")
  $("#order_id").val("#{@order.id}")
- else
  $('#message').html("#{escape_javascript(render 'shared/messages')}")
